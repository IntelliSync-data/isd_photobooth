name: Update ISD Photobooth KClickPhoto

on:
  push:
    branches:
      - kclickphoto

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy isd_photobooth to Server
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.KCLICKPHOTO_HOST }}
        username: ${{ secrets.KCLICKPHOTO_USERNAME }}
        key: ${{ secrets.KCLICKPHOTO_SSH_KEY }}
        script_stop: true
        script: |
          # Clone if not exists, otherwise update
          if [ ! -d /home/odoo/addons-system/isd_photobooth ]; then
            sudo -u odoo git clone git@github-odoo:IntelliSync-data/isd_photobooth.git /home/odoo/addons-system/isd_photobooth
            cd /home/odoo/addons-system/isd_photobooth && git checkout kclickphoto
          else
            cd /home/odoo/addons-system/isd_photobooth
            sudo -u odoo git fetch origin && sudo -u odoo git reset --hard origin/kclickphoto
          fi

          # Sync isd_photobooth directory with rsync (including deletions)
          sudo mkdir -p /opt/custom-addons/isd_photobooth
          sudo rsync -av --delete \
            /home/odoo/addons-system/isd_photobooth/ /opt/custom-addons/isd_photobooth/

    - name: Update isd_photobooth Module
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.KCLICKPHOTO_HOST }}
        username: ${{ secrets.KCLICKPHOTO_USERNAME }}
        key: ${{ secrets.KCLICKPHOTO_SSH_KEY }}
        script: |
          echo "Updating apps list..."
          echo "env['ir.module.module'].update_list(); env.cr.commit()" | sudo -u odoo /odoo-venv/bin/python /odoo18/odoo-bin shell -c /odoo.conf -d ${{ secrets.KCLICKPHOTO_DB_NAME }} --no-http
          echo "Updating isd_photobooth module..."
          sudo -u odoo /odoo-venv/bin/python /odoo18/odoo-bin -c /odoo.conf -d ${{ secrets.KCLICKPHOTO_DB_NAME }} -u isd_photobooth --stop-after-init --no-http || \
            echo "Module not installed yet (now visible in Apps list), continuing..."

    - name: Restart Odoo Service
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.KCLICKPHOTO_HOST }}
        username: ${{ secrets.KCLICKPHOTO_USERNAME }}
        key: ${{ secrets.KCLICKPHOTO_SSH_KEY }}
        script: |
          sudo systemctl restart odoo

    - name: Verify Odoo Service
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.KCLICKPHOTO_HOST }}
        username: ${{ secrets.KCLICKPHOTO_USERNAME }}
        key: ${{ secrets.KCLICKPHOTO_SSH_KEY }}
        script: |
          sleep 5
          if sudo systemctl is-active --quiet odoo; then
            echo "Odoo restarted successfully"
          else
            echo "Odoo failed to restart"
            echo "=== systemctl status ==="
            sudo systemctl status odoo --no-pager
            echo "=== Last 50 lines of Odoo log ==="
            sudo tail -n 50 /var/log/odoo/odoo.log
            exit 1
          fi
